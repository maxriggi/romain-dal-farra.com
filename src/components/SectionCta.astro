---
interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<div class="section-cta">
  <a href={`${base}${href}`} class="btn-shutter">
    {label}
  </a>
</div>

<style>
  .section-cta {
    display: flex;
    justify-content: center;
    padding: 1.5rem 0 0;
  }
</style>
